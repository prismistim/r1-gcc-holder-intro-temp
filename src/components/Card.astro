---
import type { Card } from '../types/card'

type Props = Card

const { imageUrl, title, body } = Astro.props
---

<div class="shadow-lg p-8 rounded-lg">
  <h3 class="text-lg md:text-xl font-semibold text-primary">{title}</h3>
  <p class="mt-2 text-sm md:text-base">{body}</p>
</div>
